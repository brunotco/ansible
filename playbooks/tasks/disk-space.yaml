---
- name: get disk space
  shell: df -h / | awk 'NR==2 {print $5}'
  register: disk_usage
- set_fact:
    msg:
      title: "Disk Usage"
      text: "{{disk_usage.stdout}}"
- include_tasks: notification/notify-debug.yaml
- include_tasks: notification/notify-discord.yaml